import React from 'react';
import initializeFirebaseConfigaration from '../Firebase/firebase.init';

initializeFirebaseConfigaration();

const useFirebase = () => {
    return (
        <div>

        </div>
    );
};

export default useFirebase;